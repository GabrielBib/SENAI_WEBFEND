let cateto1 = 20;
let cateto2 = 30;

// let raizQuadrada = Math.sqrt(Math.pow(cateto1, 2) + Math.pow(cateto2, 2));

// console.log(raizQuadrada.toFixed(3));

console.log(Math.hypot(cateto1, cateto2));
